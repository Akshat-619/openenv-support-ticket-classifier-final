name: support-ticket-classifier
description: OpenEnv-style environment for classifying support tickets into billing, shipping, or technical.
version: "1.0.0"

endpoints:
  home: /
  reset: /reset
  state: /state
  step: /step
  auto_step: /auto_step

methods:
  home: GET
  reset: POST
  state: GET
  step: POST
  auto_step: POST
